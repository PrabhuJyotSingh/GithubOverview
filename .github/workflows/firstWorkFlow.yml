name: first workflow

on:
  workflow_dispatch:
  
jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
     - name: firstjob script
       shell: bash
       run: echo Hello World! This is the first job
